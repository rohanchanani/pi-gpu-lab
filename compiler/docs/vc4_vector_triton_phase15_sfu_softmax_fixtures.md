PHASE15_CONTROLLED_TRITON_SFU_SOFTMAX_FIXTURES=YES
PHASE15_VALUE_APPROX_MATH_SFU_SOFTMAX_CONTRACT=LOCKED
REAL_TRITON_SFU_SOFTMAX_SOURCES=YES
REAL_TTIR_SFU_SOFTMAX_SNAPSHOTS=YES
ACCEPTED_FIXTURES_EXCLUDE_UNRELATED_STAGED_FEATURES=YES
VALUE_APPROX_SFU_EXP_SURFACE=ACCEPTED
VALUE_APPROX_SFU_RECIP_DIV_SURFACE=ACCEPTED
VALUE_FINITE_F32_MAX_REDUCTION_SURFACE=ACCEPTED
VALUE_SOFTMAX_V0_COMPOSITE_SURFACE=ACCEPTED
APPROX_MATH_POLICY=EXPLICIT
EXACT_DEFAULT_MATH_REJECTED=YES
ZERO_ACTIVE_SOFTMAX_STATUS=STAGED_OR_EXPLICIT_NOOP_GUARD_REQUIRED
EXACT_DEFAULT_MATH_FIXTURE_STAGED=YES
ZERO_ACTIVE_SOFTMAX_STAGED_OR_GUARD_REQUIRED=YES
MULTIBLOCK_SOFTMAX_STAGED=YES
READY_FOR_PHASE15_4_VALUE_SFU_SOFTMAX_STATIC=YES
READY_FOR_PHASE15_3_VALUE_SURFACE_CONTRACT=YES
READY_FOR_TRITON=NO

# Phase 15 Controlled Triton SFU/Softmax Fixtures

Phase 15.2 adds source-controlled real Triton fixtures and emitted TTIR
snapshots for the approximate-SFU math and one-block softmax contract.

The accepted snapshots are narrow:

- f32 `tl.exp` as `math.exp`;
- f32 reciprocal/division as `arith.divf` under an explicit approximate-SFU
  policy;
- finite f32 `tl.max` as `tt.reduce` with an `arith.maxnumf` combiner;
- one-block stable softmax for `1 <= ncols <= 16`;
- mixed f16-storage input softmax with f32 compute/output;
- optional `tl.log` and `tl.rsqrt`, both lower-half SFU modes already locked
  by VC4Kernel Surface v2.

Accepted fixtures exclude `tl.dot`, `tt.dot`, `vector.contract`, atomics, block
pointers, full attention, FlashAttention, multiblock softmax, fp-to-int casts,
native f16 arithmetic, bf16/fp8, and quantized int storage.

Exact/default math remains staged because VC4 SFU lowering is approximate and
requires explicit policy. Zero-active softmax remains staged unless a later
phase introduces and proves a finite no-op guard. Multiblock softmax remains
staged outside the one-block v0 contract.

The snapshots are generated by the existing pinned Triton v3.7.0 wrapper:

`.vc4_auto/vector_triton_phase85r15_generator_restore/run_vc4_emit_ttir.sh`

No compiler semantic source changes are part of this fixture-design package,
and global Triton readiness remains `NO`.

## Phase 15.3 Value Contract Handoff

Phase 15.3 locks the value-surface contract for these controlled TTIR forms
before executable lowering:

- explicit approximate-SFU policy metadata is required for `math.exp`,
  `math.log`, `math.rsqrt`, and reciprocal/division;
- finite f32 max reduction is accepted for `vector.reduction <maxnumf>` and
  `vector.reduction <maximumf>` over `vector<16xf32>`;
- one-block softmax v0 is a standard value composite, not a new
  `vc4value.softmax` op;
- exact/default math, zero-active softmax without a finite no-op guard, and
  multiblock softmax remain staged.

No executable SFU or softmax lowering is implemented by the value contract
handoff. Hardware proof remains for later Phase 15 packages, and
`READY_FOR_TRITON=NO` remains locked.
