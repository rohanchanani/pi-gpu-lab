# Metadata-only M4 slice 03 has no active VC4Tile hardware fixtures.  The
# candidate runner writes generated artifacts under .vc4_auto and does not
# mutate checked-in expected JSON/QASM/shader files.
config.unsupported = True
