#ifndef MANDELBROT_MASKED_LOOP_MAILBOX_H
#define MANDELBROT_MASKED_LOOP_MAILBOX_H

#include <stdint.h>

static inline uint32_t mandelbrot_masked_loop_mailbox_identity(uint32_t value) {
return value;
}

#endif
