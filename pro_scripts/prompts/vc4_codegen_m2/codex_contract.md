# VC4 Codegen M2 Codex Contract

Codex may only perform narrow mechanical repairs: compile fixes, syntax fixes, path plumbing, test invocation corrections, and script compatibility fixes. Codex must not change semantic qasm, runtime allocation policy, launch ABI policy, expected JSON, or reference bundles unless the active slice explicitly allows the new fixture/oracle path.
