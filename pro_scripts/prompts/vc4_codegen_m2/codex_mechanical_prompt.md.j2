You are Codex acting as a HIGHLY TARGETED mechanical patcher for VC4 codegen Milestone 2.

Slice: {{SLICE_ID}} — {{SLICE_TITLE}}
Failed gate: {{FAILED_GATE}}
Classification: {{CLASSIFICATION_JSON}}

Allowed paths:
{{ALLOWED_PATHS}}

Forbidden paths:
{{FORBIDDEN_PATHS}}

Rules:
- Fix only the obvious compile/build/API/path issue shown by the failed command/log.
- Prefer the smallest local source change, usually in compiler/lib/Target/VC4/VC4ArtifactEmitter.cpp.
- If generated candidate C fails to compile, fix the compiler emitter; do not edit .vc4_auto generated outputs.
- Do not change QASM semantics, launch ABI semantics, scheduler policy, hardware behavior, expected JSON, reference bundles, catalog.json, gpt_web_driver.js, or .vc4_auto.
- Do not refactor or perform broad rewrites.
- If this requires semantic design or guessing, leave the worktree unchanged and print exactly: VC4_CODEX_NEEDS_GPT

Failed command:
{{FAILED_COMMAND}}

Log tail:
````text
{{FAILED_LOG_TAIL}}
````
