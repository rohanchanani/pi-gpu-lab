# VC4 Codegen Milestone 1 — GPT Pro Slice Implementation Prompt

Generated: {{GENERATED_AT_UTC}}

Slice: `{{SLICE_ID}}` — {{SLICE_TITLE}}

Attempt: {{ATTEMPT}}

Mode: {{MODE}}

Template: `{{TEMPLATE_PATH}}`

You are implementing exactly one bounded slice of the VC4 codegen Milestone 1 workflow. Your output will be staged under `.vc4_auto/codegen_m1/staging/...`, validated by `pro_scripts/vc4_codegen_patch_gate.py`, and only then applied to the repo.

## Task

{{SLICE_INTENT}}

## Allowed paths

{{ALLOWED_PATHS}}

## Forbidden paths

{{FORBIDDEN_PATHS}}

## Declared gates

{{GATES}}

## Repository capability snapshot

The following machine-generated snapshot is the executable contract for local tool, CMake target, and lit substitution availability. Do not guess tool names or lit substitutions; use this snapshot and the slice gates. Any changed RUN line that uses a custom `%token` must define that token in lit config, and any bare project tool must be resolvable through PATH, `compiler/build/bin`, or a CMake tool target.

{{REPO_CAPABILITY_SNAPSHOT}}

## Slice non-goals

{{NON_GOALS}}

## Hard project constitution

{{CONSTITUTION}}

## Slice contract

{{SLICE_CONTRACT}}

## Output contract

{{OUTPUT_CONTRACT}}

## Downloadable artifact contract

{{DOWNLOAD_CONTRACT_MARKDOWN}}

Machine-readable contract:

{{DOWNLOAD_CONTRACT_JSON}}

You must create these exact downloadable files:

1. `{{BUNDLE_ZIP_FILENAME}}`
2. `{{APPLY_SCRIPT_FILENAME}}`

Do **not** paste a patch. Do **not** paste full source files. Do **not** use GPTWEB file blocks for this implementation response. The zip is the source of truth.

The zip must contain `manifest.json` and `repo/<changed paths>`. `manifest.json` must follow this schema shape:

{{RESPONSE_JSON_SCHEMA}}

The shell script must be only a tiny launcher delegating to `pro_scripts/vc4_codegen_download_bundle_apply.py` with the exact zip filename, active slice id, and attempt number. Do not put source code in the shell script.

## Executable repo/test/tool contract

{{REPO_CAPABILITIES}}

## Deterministic repo capability snapshot

{{REPO_CAPABILITIES_MARKDOWN}}

{{REPO_CAPABILITIES_JSON}}

## Executable workflow invariants

After your bundle is applied, deterministic preflight will inspect changed tests, lit RUN lines, lit config syntax, metadata, and tool/CMake wiring before expensive gates run. Do not rely on prompt prose alone. Make the repo contract true in files.

## Context pack

{{CONTEXT_PACK}}

## Context metadata

{{CONTEXT_METADATA_JSON}}
